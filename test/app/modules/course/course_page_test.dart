import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:play_cred/app/modules/course/course_page.dart';

void main() {
  testWidgets('CoursePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CoursePage(title: 'Course')));
    //  final titleFinder = find.text('Course');
    //  expect(titleFinder, findsOneWidget);
  });
}
