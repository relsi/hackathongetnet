import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:play_cred/app/modules/business/business_page.dart';

void main() {
  testWidgets('BusinessPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(BusinessPage(title: 'Business')));
    //  final titleFinder = find.text('Business');
    //  expect(titleFinder, findsOneWidget);
  });
}
