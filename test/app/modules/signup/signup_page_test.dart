import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:play_cred/app/modules/signup/signup_page.dart';

void main() {
  testWidgets('SignupPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SignupPage(title: 'Signup')));
    //  final titleFinder = find.text('Signup');
    //  expect(titleFinder, findsOneWidget);
  });
}
