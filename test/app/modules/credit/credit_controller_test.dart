import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:play_cred/app/modules/credit/credit_controller.dart';
import 'package:play_cred/app/modules/credit/credit_module.dart';

void main() {
  initModule(CreditModule());
  // CreditController credit;
  //
  setUp(() {
    //     credit = CreditModule.to.get<CreditController>();
  });

  group('CreditController Test', () {
    //   test("First Test", () {
    //     expect(credit, isInstanceOf<CreditController>());
    //   });

    //   test("Set Value", () {
    //     expect(credit.value, equals(0));
    //     credit.increment();
    //     expect(credit.value, equals(1));
    //   });
  });
}
