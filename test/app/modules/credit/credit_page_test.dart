import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:play_cred/app/modules/credit/credit_page.dart';

void main() {
  testWidgets('CreditPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CreditPage(title: 'Credit')));
    //  final titleFinder = find.text('Credit');
    //  expect(titleFinder, findsOneWidget);
  });
}
