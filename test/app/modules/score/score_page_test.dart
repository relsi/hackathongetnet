import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:play_cred/app/modules/score/score_page.dart';

void main() {
  testWidgets('ScorePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ScorePage(title: 'Score')));
    //  final titleFinder = find.text('Score');
    //  expect(titleFinder, findsOneWidget);
  });
}
