import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:play_cred/app/modules/score/score_controller.dart';
import 'package:play_cred/app/modules/score/score_module.dart';

void main() {
  initModule(ScoreModule());
  // ScoreController score;
  //
  setUp(() {
    //     score = ScoreModule.to.get<ScoreController>();
  });

  group('ScoreController Test', () {
    //   test("First Test", () {
    //     expect(score, isInstanceOf<ScoreController>());
    //   });

    //   test("Set Value", () {
    //     expect(score.value, equals(0));
    //     score.increment();
    //     expect(score.value, equals(1));
    //   });
  });
}
